[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "quilt-multi-oracle"
version = "0.1.0"
description = "Multi-model JEV oracle"
readme = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["quilt_multi_oracle"]
